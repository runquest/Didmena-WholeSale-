<div class="container">
  <div class="row">
    <div class="col s6">
      <h4><%= t('company.new')%></h4>
    </div>
  </div>

  <%= render 'form' %>

  <%= link_to t('company.back'), companies_path %>
</div>