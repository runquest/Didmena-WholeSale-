<div class="container">
  <div class="row">
    <div class="col s6">
      <h4><%= t('users.edit') %></h4>
      <%= link_to t('show'), @user %> | <%= link_to t('back'), users_path %>
    </div>
  </div>
  <%= render 'form' %>
</div>